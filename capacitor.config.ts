import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eagledashgames.eagledash',
  appName: 'Eagle Dash',
  webDir: 'dist'
};

export default config;
